// import { courseExtras } from '../courseExtras';
// import { Link } from 'react-router-dom';

// {Object.entries(courseExtras).map(([slug, course]) => (
//   <Link
//     key={slug}
//     to={`/courses/${slug}`}
//     className="back2course w-60 text-center"
//   >
//     {course.title}
//   </Link>
// ))}
